# 🌸 Iris Classification & Visualization Project

这是一个基于 Python 的鸢尾花（Iris）数据集分类与可视化项目。本项目包含四个主要任务，涵盖了从基础的 2D 分类边界可视化到复杂的 3D 交互式图表及动态切片动画的生成。

## 📂 项目结构

本项目包含以下核心脚本及输出文件：

| 文件名 | 描述 |
| :--- | :--- |
| `task1.py` | **2D 分类器比较**：在二维平面（花萼长度 vs 花萼宽度）上比较不同分类器（逻辑回归、梯度提升等）的决策边界。 |
| `task2.py` | **3D 线性决策边界**：使用 Plotly 生成交互式 3D 图表，展示多分类逻辑回归在三维特征空间中的线性决策平面。 |
| `task3.py` | **3D 非线性决策曲面**：使用 SVM (RBF核) 对 Setosa 和 Virginica 两类进行分类，并绘制 3D 概率差值曲面。 |
| `task4.py` | **3D 边界切片动画**：生成动态 GIF，展示三维决策边界随第三个特征（花瓣长度）变化时的二维切片演变过程。 |
| `iris_3d_boundary.html` | 由 `task2.py` 生成的交互式 3D 可视化网页文件。 |
| `task4_iris_3d_slicing.gif` | 由 `task4.py` 生成的决策边界切片动画。 |

## 🚀 快速开始

### 1. 克隆项目

首先，将项目克隆到本地：

```bash
git clone https://github.com/ruihaoGitHub/Iris_Classification.git
cd Iris_Classification
```

### 2. 环境配置

- **Python 版本**: 3.10+

请确保您的 Python 环境中安装了以下依赖库。您可以使用提供的 `requirements.txt` 文件进行安装：

```bash
pip install -r requirements.txt
```

### 3. 运行任务

#### 1. 运行 Task 1 (2D 分类比较)
该脚本将展示不同模型在二维特征空间下的分类效果。
```bash
python task1.py
```

#### 2. 运行 Task 2 (3D 交互式图表)
该脚本会生成 `iris_3d_boundary.html` 文件，请在浏览器中打开该文件以查看交互式 3D 决策边界。
```bash
python task2.py
```

#### 3. 运行 Task 3 (SVM 3D 曲面)
该脚本将弹出一个窗口，展示 SVM RBF 核函数的 3D 概率曲面。
```bash
python task3.py
```

#### 4. 运行 Task 4 (切片动画)
该脚本会生成 `task4_iris_3d_slicing.gif` 动画文件，展示决策边界的动态变化。
```bash
python task4.py
```

## 📊 任务详情

### Task 1: 2D Classifier Comparison
- **特征**: Sepal Length, Sepal Width
- **模型**: 
  - Logistic Regression (RBF features)
  - Gradient Boosting
  - Logistic Regression (Binned features)
  - Logistic Regression (Spline features)
- **输出**: Matplotlib 静态图表，展示不同模型的决策区域。

### Task 2: 3D Linear Decision Boundaries
- **特征**: Sepal Length, Sepal Width, Petal Length
- **模型**: Multinomial Logistic Regression
- **可视化**: 使用 Plotly 构建，支持旋转、缩放。展示了三个类别之间的线性分割平面。

### Task 3: 3D Non-linear Decision Surface
- **特征**: Sepal Length, Petal Length
- **类别**: Setosa vs Virginica
- **模型**: SVM with RBF kernel
- **可视化**: 3D 曲面图，高度轴表示预测概率的差值 (P(Virginica) - P(Setosa))，直观展示非线性边界。

### Task 4: 3D Decision Boundary Slicing
- **特征**: Sepal Length, Sepal Width, Petal Length (作为切片轴)
- **可视化**: 动态 GIF。固定第三个特征（Petal Length）的不同值，观察前两个特征平面上决策边界的变化，从而理解高维模型的行为。